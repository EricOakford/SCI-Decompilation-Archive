;;; Sierra Script 1.0 - (do not remove this comment)
;
; * SCI Script Compiler Header
; * By Brian Provinciano
(include keys.sh)

; This is now just a stub for the TRUE and FALSE defines,
; as well as the nested keys.sh (which will still be used for compatibility
; purposes)

;
; * General
(define TRUE 1)
(define FALSE 0)